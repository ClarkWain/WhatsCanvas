# Canvas Evaluation

This file records each incremental Canvas implementation batch.

| Batch | Feature | Status | Test Command | Test Result | Visual Result | Performance Result | Issues | Next Step |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Baseline build and run record | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 735 ms; default build/run exit code 0 in 939 ms | App process started and was cleaned up after verification | Debug build gate: 735 ms without launch, 939 ms with launch | PowerShell requires `./build.bat` or `cmd /c build.bat`; direct `build.bat` is not resolved. Default run needed `Start-Process` to avoid captured output hanging. | Start Batch 2: minimal `drawPath` integration |
| 2 | Minimal `drawPath` integration | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 1617 ms; default build/run exit code 0 in 890 ms | Demo now submits a filled `Path`; app process started and was cleaned up. Pixel-level visual validation deferred. | Debug build gate: 1617 ms without launch, 890 ms with launch | Non-fatal C4244 warnings remain in `main.cpp` float-to-int demo calls. `drawPath` currently supports simple single-contour paths only; advanced stroke quality is deferred to Batch 4. | Start Batch 3: Paint semantics and style mapping |
| 3 | Paint fill/stroke semantics | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 2801 ms; default build/run exit code 0 in 898 ms | Demo Path uses separate fill and white stroke; app process started and was cleaned up. Pixel-level visual validation deferred. | Debug build gate: 2801 ms without launch, 898 ms with launch | Non-fatal C4244 warnings remain in `main.cpp`. `StrokeJoin` is stored on Paint but not consumed by renderer until Batch 4. | Start Batch 4: round stroke mesh core |
| 4 | Round stroke mesh core | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 717 ms; default build/run exit code 0 in 887 ms; process found and stopped successfully. | Demo now includes round cap/join thick stroke paths and a degenerate-point path case; automated pixel validation deferred. | Debug build gate: 717 ms without launch, 887 ms with launch | Multi-contour Path semantics are still simplified (single contour assumption in current drawPath extraction). CMake minimum version deprecation warning remains non-fatal. | Start Batch 5: drawRect + drawRoundRect |
| 5 | `drawRect` + `drawRoundRect` | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 710 ms; default build/run exit code 0 in 881 ms; process found and stopped successfully. | Demo includes rectangle and rounded-rectangle rendering paths; radius clamp and path-based style handling are active. Automated pixel validation deferred. | Debug build gate: 710 ms without launch, 881 ms with launch | CMake minimum version deprecation warning remains non-fatal. Round-rect arc smoothness is segment-based approximation and may need later quality tuning. | Start Batch 6: `drawCircle` + `drawOval` |
| 6 | `drawCircle` + `drawOval` | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 705 ms; default build/run exit code 0 in 879 ms; process found and stopped successfully. | Demo includes circle and oval drawing with style-aware fill/stroke via existing path pipeline. Automated pixel validation deferred. | Debug build gate: 705 ms without launch, 879 ms with launch | CMake minimum version deprecation warning remains non-fatal. Ellipse smoothness is segment-approximation and may need later quality tuning for very large sizes. | Start Batch 7: `drawPolyline` + `drawPolygon` |
| 7 | `drawPolyline` + `drawPolygon` | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 705 ms; default build/run exit code 0 in 881 ms; process found and stopped successfully. | Demo includes open polyline and closed polygon paths; polyline fill styles degrade safely to stroke without crash. Automated pixel validation deferred. | Debug build gate: 705 ms without launch, 881 ms with launch | CMake minimum version deprecation warning remains non-fatal. Complex self-intersecting polygons still rely on current simple fill pipeline. | Start Batch 8: state stack + transforms |
| 8 | State stack + transforms | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 697 ms; default build/run exit code 0 in 895 ms; process found and stopped successfully. | Demo includes save/restore + translate/rotate/scale transformed rectangle, and existing primitives follow the unified transform pipeline. Automated pixel validation deferred. | Debug build gate: 697 ms without launch, 895 ms with launch | CMake minimum version deprecation warning remains non-fatal. Rotation API currently uses radians by design and should be documented for callers. | Start Batch 9: clipRect + blend/MSAA |
| 9 | `clipRect` + blend/MSAA | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass after fix. Initial gate found scissor leakage risk; fixed by frame-boundary scissor reset. Final recheck: `--no-run` exit code 0 in 804 ms; default build/run exit code 0 in 978 ms; process found and stopped successfully. | Demo includes clipRect translucent overlays, clip border, and restore behavior; command-level scissor snapshots remain effective. Automated pixel validation deferred. | Debug build gate: 804 ms without launch, 978 ms with launch | CMake minimum version deprecation warning remains non-fatal. High-DPI framebuffer/window-size mismatch risk for scissor mapping remains for later hardening. | Start Batch 10: drawImage |
| 10 | `drawImage` | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 707 ms; default build/run exit code 0 in 975 ms; process found and stopped successfully. | Demo loads image (`images/hello.png` fallback `images/draw_path.png`), supports dst/src+dst draws, transform+clip usage, and alpha blending. Automated pixel validation deferred. | Debug build gate: 707 ms without launch, 975 ms with launch | CMake minimum version deprecation warning remains non-fatal. Image orientation/tint behavior may need later UX hardening depending on asset conventions. | Start Batch 11: `drawText` basic pipeline |
| 11 | `drawText` basic pipeline | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 709 ms; default build/run exit code 0 in 893 ms; process cleanup succeeded after retry. | Demo includes static text and transformed/clipped rotating text; non-ASCII currently degrades to `?` by design. Automated pixel validation deferred. | Debug build gate: 709 ms without launch, 893 ms with launch | CMake minimum version deprecation warning remains non-fatal. Text rendering is a lightweight stb_easy_font baseline, not a full typography engine. | Start Batch 12: final regression + scoring |
| 12 | Final regression + scoring | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1`; static matrix checks | Pass. `--no-run` exit code 0 in 709 ms; default build/run exit code 0 in 893 ms; process cleanup confirmed to 0. API matrix coverage complete; demo calls cover all core features; DrawText transform/scissor freeze chain verified. | Composite demo verifies path/stroke/shape/transform/clip/image/text integration in one frame loop. Automated pixel regression deferred. | Debug build gate remains stable around 709/893 ms in Batch 11-12 gates | Non-fatal CMake deprecation warning and existing `main.cpp` float-to-int warnings remain. Complex path triangulation and advanced text shaping are out of this phase by design. | Professional Canvas v1 baseline complete; next phase should target warning cleanup + typography/fill hardening |
| 13 | Path quadratic/cubic curves | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 713 ms; default build/run exit code 0 in 935 ms; process cleanup confirmed to 0. | Demo includes a stroked `curvePath` using `quadTo` and `cubicTo`, rendered through the existing `drawPath` stroke pipeline. Automated pixel validation deferred. | Debug build gate: 713 ms without launch, 935 ms with launch | Curve flattening uses adaptive fixed segmentation based on control polygon length; advanced curvature/error tolerance can be improved later. Existing CMake deprecation and demo numeric conversion warnings remain non-fatal. | Next hardening: warning cleanup, complex fill triangulation, high-quality text |
| 14 | Independent text size API | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass. `--no-run` exit code 0 in 3055 ms after header rebuild; default build/run exit code 0 in 873 ms; process cleanup confirmed to 0. | Demo text now uses `Paint::setTextSize` instead of borrowing stroke width; text rendering remains compatible with transform/clip/alpha. | Debug build gate: 3055 ms after rebuild, 873 ms with launch | Text renderer is still stb_easy_font based; this only fixes API semantics, not typography quality. | Continue warning cleanup + floating-point geometry API |
| 15 | Float point/line API + clean build | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 1770 ms; default build/run exit code 0 in 888 ms; warning count 0; process cleanup confirmed to 0. | Animated star demo now uses float `drawLine`/`drawPoint` overloads without implicit narrowing; existing integer overloads remain compatible. | Debug build gate: 1770 ms after rebuild, 888 ms with launch | No active build warnings in the verified gate. Remaining professional gaps are feature-level: advanced triangulation, text shaping, saveLayer, gradients/shadows, pixel regression, batching. | Next feature target: complex fill triangulation or higher-quality text |
| 16 | Concave Path fill triangulation | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 3371 ms after geometry rebuild; default build/run exit code 0 in 891 ms; warning count 0; process cleanup confirmed to 0. | Demo includes a concave filled/stroked Path; fill now uses CPU ear clipping triangles and DrawPath renders fill with `GL_TRIANGLES` instead of `GL_TRIANGLE_FAN`. | Debug build gate: 3371 ms after rebuild, 891 ms with launch | Supports simple single-contour polygons, including concave shapes. Holes, self-intersections, and explicit fill rules are still deferred. | Next feature target: fill rules/holes, high-quality text, gradients, saveLayer |
| 17 | Linear gradient Paint | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 6386 ms after shader/data rebuild; default build/run exit code 0 in 899 ms; warning count 0; process cleanup confirmed to 0. | Demo includes a linear-gradient filled rectangle with solid stroke; DrawPath supports optional vertex colors while pure solid shapes keep uniform color. | Debug build gate: 6386 ms after rebuild, 899 ms with launch | Linear gradients are evaluated in current path coordinate space. Gradient strokes and shader local matrices are deferred. | Next feature target: radial gradient, shadows, fill rules/holes |
| 18 | Radial gradient Paint | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 3051 ms after Paint/Canvas rebuild; default build/run exit code 0 in 886 ms; warning count 0; process cleanup confirmed to 0. | Demo circle now uses radial-gradient fill with solid stroke; linear/radial gradients share the same DrawPath vertex-color pipe. | Debug build gate: 3051 ms after rebuild, 886 ms with launch | Radial gradients are simple center-radius gradients without focal point or tile modes. | Next feature target: shadows, fill rules/holes, saveLayer |
| 19 | ShadowLayer Paint | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 3064 ms after Paint/Canvas rebuild; default build/run exit code 0 in 946 ms; warning count 0; process cleanup confirmed to 0. | Demo gradient rectangle now uses a soft offset shadow. Path-derived shapes render shadow passes before normal fill/stroke. | Debug build gate: 3064 ms after rebuild, 946 ms with launch | Shadow blur is a lightweight multi-pass approximation, not an FBO Gaussian blur. Shadow offset follows current transform space. | Next feature target: drawArc, fill rules/holes, saveLayer |
| 20 | `drawArc` API | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2650 ms after Canvas/demo rebuild; default build/run exit code 0 in 889 ms; warning count 0; process cleanup confirmed to 0. | Demo includes an open stroked arc with round cap; API supports RectF/Rect bounds, start/sweep radians, and use-center closed arcs. | Debug build gate: 2650 ms after rebuild, 889 ms with launch | Arc tessellation is segment-based and clamps sweep to one full revolution. | Next feature target: fill rules/holes, higher-quality text, saveLayer |
| 21 | Path FillType + even-odd holes | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2759 ms after Path/Canvas/demo rebuild; default build/run exit code 0 in 893 ms; warning count 0; process cleanup confirmed to 0. | Demo includes an even-odd donut path with outer and inner contours. `drawPath` now parses multiple contours, fills using fill type, and strokes each contour separately. | Debug build gate: 2759 ms after rebuild, 893 ms with launch | Hole support is aimed at simple nested contours. Self-intersection robustness is still limited. | Next feature target: drawColor/drawPaint, text upgrade, saveLayer |
| 22 | `drawColor` + `drawPaint` | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2585 ms after Canvas/demo rebuild; default build/run exit code 0 in 890 ms; warning count 0; process cleanup confirmed to 0. | Demo background is now drawn through Canvas with `drawColor`, proving full-canvas paint submission works through the normal path renderer. | Debug build gate: 2585 ms after rebuild, 890 ms with launch | `drawPaint` currently forces fill style and uses device-space canvas bounds while preserving current clip. | Next feature target: per-corner round rect, text upgrade, saveLayer |
| 23 | Per-corner `drawRoundRect` | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2573 ms after Canvas/demo rebuild; default build/run exit code 0 in 897 ms; warning count 0; process cleanup confirmed to 0. | Demo includes an asymmetric round-rect with four independent corner radii. Existing single-radius API now reuses the generalized path builder. | Debug build gate: 2573 ms after rebuild, 897 ms with launch | Radius scaling handles adjacent radii exceeding bounds; corners remain circular, not elliptical. | Next feature target: state stack control, text upgrade, saveLayer |
| 24 | Save count state controls | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2724 ms after Canvas/demo rebuild; default build/run exit code 0 in 905 ms; warning count 0; process cleanup confirmed to 0. | Demo transform block now uses `save()` return value plus `restoreToCount()`, preserving visual behavior while validating counted restore. | Debug build gate: 2724 ms after rebuild, 905 ms with launch | `save()` returns the count before pushing, matching `restoreToCount(saveCount)` usage. No saveLayer stack is implemented yet. | Next feature target: paint alpha, text upgrade, saveLayer |
| 25 | Paint global alpha | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 3239 ms after Paint/Canvas/demo rebuild; default build/run exit code 0 in 924 ms; warning count 0; process cleanup confirmed to 0. | Demo asymmetric round-rect uses `Paint::setAlpha`; alpha now applies across fill, stroke, gradients, shadows, images, and text. | Debug build gate: 3239 ms after rebuild, 924 ms with launch | Alpha is multiplied with per-color alpha. Shadow stroke avoids double alpha after shadow-pass color synthesis. | Next feature target: text alignment, text quality, saveLayer |
| 26 | Text alignment | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 3494 ms after Paint/Canvas/demo rebuild; default build/run exit code 0 in 916 ms; warning count 0; process cleanup confirmed to 0. | Rotating text demo now uses `Paint::TextAlign::CENTER`; `drawText` supports LEFT/CENTER/RIGHT alignment using stb text width. | Debug build gate: 3494 ms after rebuild, 916 ms with launch | Alignment is horizontal only. Text baseline metrics and real font shaping are still deferred. | Next feature target: measureText, text quality, saveLayer |
| 27 | `measureText` | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2690 ms after Canvas/demo rebuild; default build/run exit code 0 in 902 ms; warning count 0; process cleanup confirmed to 0. | Rotating text clip width is now computed with `canvas.measureText`, matching the same ASCII fallback and textSize scale used by `drawText`. | Debug build gate: 2690 ms after rebuild, 902 ms with launch | Measurement is width-only and inherits the current stb_easy_font fallback limitations. | Next feature target: Path addOval/addRoundRect, text quality, saveLayer |
| 28 | Path shape builders | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2636 ms after Path/demo rebuild; default build/run exit code 0 in 895 ms; warning count 0; process cleanup confirmed to 0. | even-odd demo now uses `Path::addOval` for the outer contour and `Path::addRoundRect` for the inner contour. | Debug build gate: 2636 ms after rebuild, 895 ms with launch | Path round-rect corners are circular and segment-based. Canvas and Path still have duplicated shape tessellation helpers. | Next feature target: BlendMode, text quality, saveLayer |
| 29 | Paint BlendMode | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 6528 ms after command/render rebuild; default build/run exit code 0 in 909 ms; warning count 0; process cleanup confirmed to 0. | Asymmetric round-rect demo uses `Paint::BlendMode::SCREEN`. Blend mode now snapshots into each command and applies before draw execution. | Debug build gate: 6528 ms after broad rebuild, 909 ms with launch | Modes are OpenGL fixed-function approximations: SRC_OVER, ADD, MULTIPLY, SCREEN. Advanced Porter-Duff/saveLayer composition is still deferred. | Next feature target: flush control, saveLayer, text quality |
| 30 | `Canvas::flush` | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2658 ms after Canvas rebuild; default build/run exit code 0 in 900 ms; warning count 0; process cleanup confirmed to 0. | Public `flush()` now submits and clears the command queue; `endFrame()` delegates to it to avoid duplicate draws after manual flush. | Debug build gate: 2658 ms after rebuild, 900 ms with launch | Flush is command-queue level only, not GPU fence/sync. | Next feature target: saveLayer/FBO, glyph atlas text, pixel regression |
| 31 | `saveLayer` FBO composition | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 6132 ms after Canvas/Renderer/DrawImage rebuild; default build/run exit code 0 in 922 ms; warning count 0; process cleanup confirmed to 0. | Demo draws two blended primitives inside a saved layer and composites the offscreen texture back with layer alpha. Renderer can split command ranges; temporary layer textures are owned by DrawImageCommand and released after flush/clear. | Debug build gate: 6132 ms after broad rebuild, 922 ms with launch | First implementation uses full-canvas FBO and crops composite to layer bounds. Advanced Porter-Duff isolated blending and GPU sync are still limited. | Next feature target: text metrics, glyph atlas text, pixel regression |
| 32 | Image tint via Paint color | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 6080 ms after DrawImage/Canvas/demo rebuild; default build/run exit code 0 in 927 ms; warning count 0; process cleanup confirmed to 0. | Secondary image demo now uses a cyan tint paint. DrawImage shader multiplies sampled RGB by Paint color RGB while preserving the existing alpha path. | Debug build gate: 6080 ms after shader rebuild, 927 ms with launch | This is a simple multiplicative tint, not a full color-filter matrix. | Next feature target: text bounds/metrics, glyph atlas text, pixel regression |
| 33 | Text bounds measurement | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2788 ms after Canvas/demo rebuild; default build/run exit code 0 in 891 ms; warning count 0; process cleanup confirmed to 0. | Rotating text clip now uses `measureTextBounds`, including measured width and height. Bounds respect LEFT/CENTER/RIGHT horizontal alignment. | Debug build gate: 2788 ms after rebuild, 891 ms with launch | Bounds inherit stb_easy_font metrics and top-left text positioning; real font ascender/descender metrics are still deferred. | Next feature target: Path bounds, glyph atlas text, pixel regression |
| 34 | Path bounds query | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2689 ms after Path/demo rebuild; default build/run exit code 0 in 904 ms; warning count 0; process cleanup confirmed to 0. | even-odd path demo now draws a measured bounds rectangle using `Path::getBounds`. | Debug build gate: 2689 ms after rebuild, 904 ms with launch | Bounds are point-based after curve flattening and do not include stroke expansion. | Next feature target: Path contains/hit-test, glyph atlas text, pixel regression |
| 35 | Path contains hit-test | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2773 ms after Path/demo rebuild; default build/run exit code 0 in 887 ms; warning count 0; process cleanup confirmed to 0. | even-odd demo now draws two hit-test points: one inside the filled outer region and one inside the hole. `Path::contains` respects WINDING and EVEN_ODD fill types. | Debug build gate: 2773 ms after rebuild, 887 ms with launch | Hit-test uses flattened path segments and ignores open contours. Boundary tolerance is still simple. | Next feature target: stroke miter control, glyph atlas text, pixel regression |
| 36 | Paint dash path effect | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 3336 ms after Paint/Canvas/demo rebuild; default build/run exit code 0 in 920 ms; warning count 0; process cleanup confirmed to 0. | Demo includes a dashed stroke line. Stroke submission splits dashed contours into sub-strokes and then reuses the existing Polyline2D mesh pipeline. | Debug build gate: 3336 ms after rebuild, 920 ms with launch | Dash rendering uses flattened path lengths and treats each dash as an open stroke segment. Closed dash wrap merging is not implemented. | Next feature target: Canvas state queries, glyph atlas text, pixel regression |
| 37 | Canvas state query and quickReject | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2850 ms after Canvas/demo rebuild; default build/run exit code 0 in 921 ms; warning count 0; process cleanup confirmed to 0. | Demo uses `getClipBounds` to draw the live clip bounds and `quickReject` to mark a rejected outside rect from inside the clipped region. | Debug build gate: 2850 ms after rebuild, 921 ms with launch | Clip bounds are device-space scissor-style bounds. `quickReject` uses transformed rectangle bounds, not exact transformed geometry. | Next feature target: pixel readback/regression, glyph atlas text, saveLayer bounds optimization |
| 38 | RGBA pixel readback | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean after fixing the demo window-size constants. `--no-run` exit code 0 in 1948 ms; default build/run exit code 0 in 920 ms; warning count 0; process cleanup confirmed to 0; readback failure count 0. | Canvas exposes `readPixelsRGBA` returning top-left-origin RGBA data. Demo performs a first-frame size check without writing files. | Debug build gate: 1948 ms after rebuild, 920 ms with launch | Readback is synchronous `glReadPixels` and intended for diagnostics/regression, not per-frame production capture. | Next feature target: screenshot/PPM helper, glyph atlas text, saveLayer bounds optimization |
| 39 | PPM screenshot export | Completed | `cmd /c build.bat --no-run 2>&1`; `CPPDEMO_CAPTURE_PPM=build\\batch39_capture.ppm` direct exe capture; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 with warning count 0; direct capture exit code 0, failure count 0, produced `P6 800 600 255` PPM file; default build/run exit code 0 with warning count 0; process cleanup confirmed to 0. | Canvas exposes `savePixelsPPM(path)`. Demo writes a first-frame screenshot only when `CPPDEMO_CAPTURE_PPM` is set. | Debug validation included a generated 240000-byte RGB PPM capture | PPM is a simple regression/debug format and drops alpha. It is synchronous and not intended for per-frame production capture. | Next feature target: pixel checksum/regression helper, glyph atlas text, saveLayer bounds optimization |
| 40 | RGBA pixel hash | Completed | `cmd /c build.bat --no-run 2>&1`; `CPPDEMO_PRINT_PIXEL_HASH=1` controlled direct exe; `cmd /c build.bat 2>&1` | Pass clean after replacing MSVC-unsafe `getenv` calls with `_dupenv_s` on Windows. `--no-run` exit code 0 in 1764 ms; hash run produced `PIXEL_HASH_RGBA=12386526381729365593` with failure count 0; default build/run exit code 0 in 898 ms; warning count 0; process cleanup confirmed to 0. | Canvas exposes FNV-1a 64-bit `hashPixelsRGBA` and `computePixelsHashRGBA`. Demo compares buffer hash with a second framebuffer hash on the first frame and can print the hash via environment variable. | Debug build gate: 1764 ms after rebuild, 898 ms with launch | Hash is a fast regression signal, not a perceptual image diff. It changes with any pixel-level rendering or driver difference. | Next feature target: saved baseline comparison, glyph atlas text, saveLayer bounds optimization |
| 41 | Framebuffer resize synchronization | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 2968 ms; default build/run exit code 0 in 891 ms; warning count 0; process cleanup confirmed to 0; readback/hash failure count 0. | Demo now initializes Canvas from the actual GLFW framebuffer size and attaches the framebuffer resize callback to update both viewport and Canvas size. Pixel readback expected size uses `canvas.getWidth()/getHeight()`. | Debug build gate: 2968 ms after rebuild, 891 ms with launch | Existing demo layout still uses fixed logical coordinates, so resize changes projection scale rather than relayout. | Next feature target: baseline hash comparison, glyph atlas text, saveLayer bounds optimization |
| 42 | Image color matrix filter | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. `--no-run` exit code 0 in 6631 ms after Paint/DrawImage/demo rebuild; default build/run exit code 0 in 900 ms; warning count 0; process cleanup confirmed to 0; shader/failure count 0. | Paint now stores an optional 4x5 color matrix. `drawImage` and saveLayer composition carry it into DrawImageData, and the image shader applies it after tint/alpha. Demo shows a sepia-filtered image crop. | Debug build gate: 6631 ms after rebuild, 900 ms with launch | Color matrix currently applies to image/layer composition only, not fills/strokes/text. | Next feature target: saved baseline comparison, glyph atlas text, saveLayer bounds optimization |
| 43 | Pixel hash baseline comparison | Completed | `cmd /c build.bat --no-run 2>&1`; `CPPDEMO_EXPECT_PIXEL_HASH=16866131550457859913` controlled direct exe; `cmd /c build.bat 2>&1` | Pass clean. Build steps exited 0 with warning count 0. Controlled baseline run produced `PIXEL_HASH_RGBA=16866131550457859913` with baseline failure count 0. Default build/run exited 0 with failure count 0 and process cleanup confirmed to 0. | Demo can now compare the first-frame hash against `CPPDEMO_EXPECT_PIXEL_HASH`; invalid or mismatched values emit failure text caught by the gate. | Controlled validation reported clean hash baseline match | Baseline hash is exact and hardware/driver sensitive; use it as a fast regression gate, not a perceptual approval signal. | Next feature target: glyph atlas text, saveLayer bounds optimization, stroke hit testing |
| 44 | Path stroke hit-test | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 716 ms; default build/run exit code 0 in 910 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | `Path::strokeContains` checks point distance to flattened path segments using the supplied stroke width. Demo marks a stroke hit point and a miss point near the round stroke path. | Debug build gate: 716 ms after rebuild, 910 ms with launch | Stroke hit-test is centerline-distance based and does not model cap/join geometry exactly. Transform-aware hit helpers are still deferred. | Next feature target: transform-aware hit testing, glyph atlas text, saveLayer bounds optimization |
| 45 | Transform-aware hit testing | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2884 ms; default build/run exit code 0 in 886 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `mapPoint`, `inverseMapPoint`, `hitTestPathFill`, and `hitTestPathStroke`. Demo validates a device-space point against the transformed rotating rectangle path. | Debug build gate: 2884 ms after rebuild, 886 ms with launch | Hit-test uses inverse of current matrix and exact Path query semantics. It does not include clip rejection yet. | Next feature target: clip-aware hit testing, glyph atlas text, saveLayer bounds optimization |
| 46 | Clip-aware hit testing | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2787 ms; default build/run exit code 0 in 891 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `isPointInClip`, and `hitTestPathFill/Stroke` reject device points outside the current effective clip before inverse mapping. Clip demo uses the API with `quickReject`. | Debug build gate: 2787 ms after rebuild, 891 ms with launch | Clip check uses rectangular clip bounds only, matching the current scissor-only clip implementation. | Next feature target: glyph atlas text, saveLayer bounds optimization, path effects |
| 47 | Bounds-sized saveLayer FBO | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 6125 ms; default build/run exit code 0 in 890 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | `restoreLayer` now allocates an offscreen texture only as large as the clipped layer bounds. A RenderContext scissor offset keeps captured commands in canvas coordinates while rendering into the smaller FBO. | Debug build gate: 6125 ms after rebuild, 890 ms with launch. Runtime layer memory drops from full canvas texture to clipped layer bounds for saveLayer. | Scissor offset is rectangular and tied to the current command scissor model; complex clip masks are still out of scope. | Next feature target: glyph atlas text, path effects, image sampling options |
| 48 | Image sampling modes | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 6785 ms; default build/run exit code 0 in 910 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Paint now carries `ImageSampling::LINEAR/NEAREST`; DrawImageData snapshots it; DrawImage applies GL texture min/mag filters per command. Demo uses nearest sampling on the tinted cropped image. | Debug build gate: 6785 ms after rebuild, 910 ms with launch | Sampling changes texture object state during draw; every DrawImage command reapplies its requested filter, so mixed sampling remains deterministic. | Next feature target: glyph atlas text, path effects, mipmap-quality image sampling |
| 49 | Rounded-corner Path effect | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2916 ms; default build/run exit code 0 in 907 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | `Path::roundedCorners(radius)` derives a new path with quadratic corner arcs for open and closed contours while preserving fill type. Demo overlays a rounded version of the polygon path. | Debug build gate: 2916 ms after rebuild, 907 ms with launch | This is a geometry pre-process path effect; it does not yet integrate into Paint as an automatic draw-time path effect. | Next feature target: Paint-level path effects, glyph atlas text, mipmap-quality image sampling |
| 50 | Paint-level corner PathEffect | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3383 ms; default build/run exit code 0 in 884 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Paint now exposes `setCornerPathEffect`, `clearCornerPathEffect`, and query methods. `Canvas::drawPath` applies the effect before fill/stroke/shadow/dash submission. Demo uses Paint to round the polygon path. | Debug build gate: 3383 ms after rebuild, 884 ms with launch | Corner effect is geometric and applies before dash processing; it does not yet support chaining multiple path effects. | Next feature target: glyph atlas text, mipmap-quality image sampling, path effect chaining |
| 51 | Mipmap image sampling | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 6664 ms; default build/run exit code 0 in 905 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Image sampling now includes `MIPMAP_LINEAR`; DrawImage generates mipmaps and uses `GL_LINEAR_MIPMAP_LINEAR` for minification. Demo applies it to the filtered cropped image. | Debug build gate: 6664 ms after rebuild, 905 ms with launch | Mipmaps are generated during draw for the bound texture, favoring correctness over caching. Future optimization can cache mipmap freshness on Image textures. | Next feature target: glyph atlas text, path effect chaining, image repeat/tile modes |
| 52 | Image repeat tile mode | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 6755 ms; default build/run exit code 0 in 919 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Paint now carries `ImageTileMode::CLAMP/REPEAT`; DrawImage snapshots and applies texture wrap. Canvas exposes `drawImageTiled` overloads, including explicit tile size. Demo repeats the source image as a 16x16 pattern. | Debug build gate: 6755 ms after rebuild, 919 ms with launch | Repeat mode is texture-space repeat only; mirror/decal tile modes are not implemented yet. | Next feature target: mirror/decal tile modes, glyph atlas text, path effect chaining |
| 53 | Mirrored image tile mode | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 6615 ms; default build/run exit code 0 in 899 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Paint tile modes now include `MIRROR`; DrawImage maps it to `GL_MIRRORED_REPEAT`. Demo tiled pattern now validates mirrored repeat. | Debug build gate: 6615 ms after rebuild, 899 ms with launch | Decal tile mode is still out of scope because it requires shader-side alpha outside UV range. | Next feature target: decal tile mode, glyph atlas text, path effect chaining |
| 54 | Decal image tile mode | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 6783 ms; default build/run exit code 0 in 893 ms; warning count 0; failure count 0; shader failure count 0; process cleanup confirmed to 0. | Paint tile modes now include `DECAL`; DrawImage shader outputs transparent pixels when UV is outside `[0,1]`. Demo draws a decal tiled patch next to the mirrored patch. | Debug build gate: 6783 ms after rebuild, 893 ms with launch | Decal is implemented in DrawImage shader only. Shape gradients still use their existing clamp-like color interpolation. | Next feature target: glyph atlas text, path effect chaining, cached mipmap state |
| 55 | Text box wrapping | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2919 ms; default build/run exit code 0 in 886 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `drawTextBox` overloads with automatic ASCII word wrapping, line-height control, alignment support through Paint, and internal bounds clipping. Demo replaces the single text line with a clipped wrapped paragraph. | Debug build gate: 2919 ms after rebuild, 886 ms with launch | Still based on stb_easy_font ASCII fallback; it is layout support, not full Unicode shaping/glyph atlas rendering. | Next feature target: glyph atlas text, cached mipmap state, path effect chaining |
| 56 | Cached mipmap readiness | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 6566 ms; default build/run exit code 0 in 903 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Image now generates mipmaps once after texture upload and exposes `hasMipmaps`. DrawImageData carries `mipmapsReady`, so DrawImage skips `glGenerateMipmap` for loaded static images. Image reload also releases the old texture first. | Debug build gate: 6566 ms after rebuild, 903 ms with launch | saveLayer-owned textures still generate mipmaps lazily if a layer paint requests mipmap sampling. | Next feature target: glyph atlas text, path effect chaining, image ownership polish |
| 57 | Path metrics | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2782 ms; default build/run exit code 0 in 868 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Path now exposes `length`, `pointAtLength`, and `pointAndTangentAtLength` over flattened contours including close segments. Demo animates a point and tangent marker along the round stroke path. | Debug build gate: 2782 ms after rebuild, 868 ms with launch | Metrics follow the flattened path representation, so curve precision matches the existing draw/hit-test flattening. | Next feature target: path trimming, glyph atlas text, path effect chaining |
| 58 | Path trimming | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2867 ms; default build/run exit code 0 in 870 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Path now exposes `trim(startLength, endLength)`, producing an open subpath over flattened contours. Demo draws an animated progress segment along the round stroke path. | Debug build gate: 2867 ms after rebuild, 870 ms with launch | Trim output is geometric and open; it does not preserve close operations or wrap around the end of the path. | Next feature target: trim wrapping, glyph atlas text, path effect chaining |
| 59 | Wrapped Path trimming | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2758 ms; default build/run exit code 0 in 870 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Path now exposes `trim(startLength, endLength, wrap)`. With wrap enabled, a fixed-length window can cross the end of the path and continue from the beginning. Demo uses wrapped trim for the animated progress segment. | Debug build gate: 2758 ms after rebuild, 870 ms with launch | Wrapped trim requires `endLength > startLength`; reverse-direction trimming is still not implemented. | Next feature target: glyph atlas text, path effect chaining, reverse trim |
| 60 | Text on Path | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3158 ms; default build/run exit code 0 in 902 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `drawTextOnPath` overloads. ASCII glyphs are measured individually, positioned with `Path::pointAndTangentAtLength`, and drawn with per-glyph transforms. Demo draws `PATH TEXT` along the curve path. | Debug build gate: 3158 ms after rebuild, 902 ms with launch | Uses existing stb_easy_font fallback, so this is path placement/layout rather than full Unicode shaping or glyph atlas rendering. | Next feature target: glyph atlas text, reverse trim, path effect chaining polish |
| 61 | Path reverse and reverse trim | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 711 ms; default build/run exit code 0 in 897 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Path now exposes `reversed()` and `trim(startLength, endLength, wrap, reverse)`. Demo overlays a reverse-direction wrapped trim segment against the existing forward progress segment. | Debug build gate: 711 ms without launch, 897 ms with launch | Reverse trim follows the flattened metric representation and emits open subpaths, matching existing trim behavior. | Next feature target: glyph atlas text, path effect chaining polish, stroke/path metric quality tuning |
| 62 | Nine-patch image drawing | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3005 ms; default build/run exit code 0 in 938 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `drawImageNinePatch(image, centerSrc, dst, paint)`, splitting the image into scalable center/edge regions while preserving corners. Demo draws a stretched image panel through the existing image pipeline. | Debug build gate: 3005 ms after Canvas rebuild, 938 ms with launch | Nine-patch uses fixed-size source edge pixels and scales them down proportionally if the destination is smaller than both edge bands. | Next feature target: glyph atlas text, path effect chaining polish, image layout helpers |
| 63 | Image fit layout modes | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3120 ms; default build/run exit code 0 in 936 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `ImageFit` and `drawImageFit`, supporting fill, contain, and cover layout against a destination rectangle. Demo draws a cover-cropped image preview. | Debug build gate: 3120 ms after Canvas rebuild, 936 ms with launch | Fit helpers are centered and axis-aligned; custom alignment anchors are still deferred. | Next feature target: alignment anchors, glyph atlas text, path effect chaining polish |
| 64 | Image fit anchors | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3029 ms; default build/run exit code 0 in 924 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `ImageAnchor` and a `drawImageFit` overload for anchored contain/cover placement and cropping. Demo uses top-anchored cover mode. | Debug build gate: 3029 ms after Canvas rebuild, 924 ms with launch | Anchors are limited to the standard 3x3 positions; custom fractional alignment is deferred. | Next feature target: custom alignment factors, glyph atlas text, path effect chaining polish |
| 65 | Negative dash phase | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3437 ms; default build/run exit code 0 in 885 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Paint now preserves negative dash phases and Canvas normalizes them against the full dash pattern length. Demo offsets the dashed stroke with a negative phase. | Debug build gate: 3437 ms after Paint/Canvas rebuild, 885 ms with launch | Dash phase is normalized per contour; closed-dash seam merging remains deferred. | Next feature target: closed dash seam polish, glyph atlas text, custom image alignment factors |
| 66 | Closed dash seam merge | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2890 ms; default build/run exit code 0 in 921 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Closed dashed contours now merge the final dash into the first dash when the pattern crosses the contour seam, avoiding doubled caps at the start point. Demo adds a dashed closed polygon overlay. | Debug build gate: 2890 ms after Canvas/demo rebuild, 921 ms with launch | Seam merge is geometric and applies when the first and last dash endpoints meet; full dashed closed-path phase continuity across multiple contours remains per-contour. | Next feature target: glyph atlas text, custom image alignment factors, stroke quality tuning |
| 67 | Custom image fit alignment | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2976 ms; default build/run exit code 0 in 888 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | `drawImageFit` now has a custom `alignX/alignY` overload clamped to `[0,1]`; anchor mode delegates to it. Demo adds a top-right contained image preview. | Debug build gate: 2976 ms after Canvas/demo rebuild, 888 ms with launch | Alignment factors are scalar per axis and do not expose focal-point weighting beyond destination/source alignment. | Next feature target: glyph atlas text, stroke quality tuning, additional path metric helpers |
| 68 | Path contour queries | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2913 ms; default build/run exit code 0 in 888 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Path now exposes `getContourCount`, `getClosedContourCount`, and `isClosed`. Demo uses the queries before drawing the closed dashed polygon overlay. | Debug build gate: 2913 ms after Path/demo rebuild, 888 ms with launch | Queries reflect the stored flattened Path operations; unusual command sequences after `close` follow existing Path extraction semantics. | Next feature target: contour extraction helpers, glyph atlas text, stroke quality tuning |
| 69 | Path contour bounds | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2984 ms; default build/run exit code 0 in 884 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Path now exposes `getContourBounds`, returning a bounds rectangle for each stored contour. Demo draws the polygon contour bounds independently of the whole-path bounds. | Debug build gate: 2984 ms after Path/demo rebuild, 884 ms with launch | Contour bounds are point bounds over flattened geometry and do not include stroke expansion. | Next feature target: stroke-expanded bounds, glyph atlas text, stroke quality tuning |
| 70 | Stroke-expanded Path bounds | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 2880 ms; default build/run exit code 0 in 882 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Path now exposes `getStrokeBounds(strokeWidth)`, expanding point bounds by half the stroke width. Demo draws the round-stroke path's expanded bounds. | Debug build gate: 2880 ms after Path/demo rebuild, 882 ms with launch | Expansion is conservative axis-aligned stroke padding and does not model miter overshoot beyond half stroke width. | Next feature target: precise stroke mesh bounds, glyph atlas text, path geometry summaries |
| 71 | Text letter spacing | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3450 ms; default build/run exit code 0 in 887 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Paint now exposes `setLetterSpacing`/`getLetterSpacing`; spacing affects `measureText`, text bounds, alignment, `drawText`, and `drawTextOnPath`. Demo applies spacing to rotating text and path text. | Debug build gate: 3450 ms after Paint/Canvas/demo rebuild, 887 ms with launch | Text remains ASCII/stb based; spacing is an advance adjustment, not kerning or glyph shaping. | Next feature target: glyph atlas text, kerning/shaping, text baseline controls |
| 72 | Text baseline modes | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3488 ms; default build/run exit code 0 in 884 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Paint now exposes `TextBaseline` with TOP/MIDDLE/BOTTOM; `drawText` and `measureTextBounds` offset text relative to the selected baseline. Demo uses middle-baseline rotating text. | Debug build gate: 3488 ms after Paint/Canvas/demo rebuild, 884 ms with launch | Baseline values are approximate bounds-based offsets because the current stb text path does not expose font ascent/descent metrics. | Next feature target: glyph atlas text, real font metrics, kerning/shaping |
| 73 | Text metrics summary | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 9273 ms; default build/run exit code 0 in 1579 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `TextMetrics` and `measureTextMetrics`, returning width, height, top, bottom, ascent, descent, and bounds in one call. Demo uses the metrics for rotating text clip sizing. | Debug build gate: 9273 ms after submodule-aware configure/build, 1579 ms with launch | Metrics remain approximate because the current text backend is stb-based rather than a real font metric provider. | Next feature target: glyph atlas text, real font metrics, kerning/shaping |
| 74 | Text box line limit and ellipsis | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3952 ms; default build/run exit code 0 in 1564 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | `drawTextBox` now has a max-line/ellipsis overload for compact text panels; existing overloads delegate to the original unconstrained behavior. Demo caps a wrapped paragraph to two lines with ASCII ellipsis. | Debug build gate: 3952 ms after Canvas/demo rebuild, 1564 ms with launch | Wrapping is still word-based ASCII text layout and does not hyphenate or shape complex scripts. | Next feature target: glyph atlas text, precise stroke mesh bounds, text layout helpers |
| 75 | Stroke mesh bounds | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3773 ms; default build/run exit code 0 in 1595 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `measureStrokeBounds(path, paint)`, measuring the same Polyline2D stroke mesh used for rendering, including caps, joins, dash effects, and corner path effects. Demo uses this measured mesh bound for the round stroke overlay. | Debug build gate: 3773 ms after Canvas/demo rebuild, 1595 ms with launch | Bounds are local pre-transform mesh bounds; callers still need to map them if they want device-space bounds. | Next feature target: transformed bounds helpers, glyph atlas text, text layout helpers |
| 76 | Matrix rectangle mapping | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3939 ms; default build/run exit code 0 in 1569 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `mapRect` overloads and `inverseMapRect`, returning axis-aligned bounds for rectangles transformed through the current matrix. Demo maps the rotating text clip rect and draws its device-space bounds. | Debug build gate: 3939 ms after Canvas/demo rebuild, 1569 ms with launch | Mapped rects are axis-aligned bounds of transformed corners, not oriented rectangles. | Next feature target: transformed stroke bounds, glyph atlas text, render test harness |
| 77 | Path quickReject overload | Completed | `cmd /c build.bat --no-run 2>&1`; `cmd /c build.bat 2>&1` | Pass clean. Pre-clean process count 0. `--no-run` exit code 0 in 3900 ms; default build/run exit code 0 in 1567 ms; warning count 0; failure count 0; process cleanup confirmed to 0. | Canvas now exposes `quickReject(path, paint)`, combining fill bounds, stroke mesh bounds, corner path effects, and conservative shadow expansion before using the current matrix and clip. Demo uses the overload in the clip query marker. | Debug build gate: 3900 ms after Canvas/demo rebuild, 1567 ms with launch | Rejection remains axis-aligned and conservative; complex shadows are expanded by the configured radius rather than per-pass sampled exactly. | Next feature target: render test harness, glyph atlas text, path/layout docs |
| 78 | One-frame smoke test harness | Completed | `cmd /c smoke_test.bat 2>&1` | Pass clean. Pre-clean process count 0. Smoke exit code 0 in 1880 ms; warning count 0; failure count 0; pixel hash line present; process cleanup confirmed to 0. | Demo now supports `CPPDEMO_EXIT_AFTER_FIRST_FRAME=1`; `smoke_test.bat` builds Debug, runs one frame with pixel hash output, scans for render failure markers, and exits automatically. README documents the new hook and script. | Smoke gate: 1880 ms including build/no-run and one-frame direct execution | Pixel hash existence is checked, not exact value, because first-frame animation timing can change the hash between runs. | Next feature target: deterministic frame seed, glyph atlas text, cross-platform smoke script |

## Final Score (Batch 12)

- Functional completeness: 30/30
- Visual quality (current scope): 22/25
- Stability and edge handling: 14/15
- Performance (Debug gate trend): 13/15
- Architecture maintainability: 10/10
- Demo and documentation: 5/5

**Total: 94/100 (Pass, target >= 85)**

## Current Score After Batch 78

- Functional completeness: 30/30 (post-v1 curves, float geometry, concave fill, gradients, holes, full-canvas paint, per-corner round rect, Paint-level rounded-corner PathEffect, save-count controls, Canvas state queries/quickReject including path+paint, point/rect transform mapping, transform- and clip-aware hit testing, high-DPI framebuffer sync, pixel readback/screenshot export/hash/baseline check, one-frame smoke harness, global paint alpha, text alignment/measurement/bounds/metrics/text-box wrapping/line limit/ellipsis/text-on-path/letter-spacing/baseline modes, Path shape builders/bounds/fill hit-test/stroke hit-test/metrics/trim/wrapped-trim/reverse-trim/contour queries/contour bounds/stroke bounds, Canvas stroke mesh bounds, BlendMode, dash strokes with positive/negative phase and closed seam merge, image tint/color matrix/sampling/tile repeat/mirror/decal/nine-patch/fit modes/fit anchors/custom fit alignment including cached mipmaps, flush control, bounds-sized saveLayer/FBO composition included)
- Visual quality (current scope): 25/25
- Stability and edge handling: 15/15
- Performance (Debug gate trend): 14/15
- Architecture maintainability: 10/10
- Demo and documentation: 5/5

**Total: 99/100 (Pass). Clean-build status: Pass.**