# Treat renderer baseline images as binary so their pixel payloads are never
# mangled by end-of-line normalization.
*.pam binary
*.ppm binary
*.png binary

# Unified diff context lines can legitimately contain a single space.
*.diff -whitespace
*.patch -whitespace
